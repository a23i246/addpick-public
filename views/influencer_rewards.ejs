<%- include('partials/header', { title: '報酬ダッシュボード' }) %>

<div class="container mt-5">
  <h1 class="mb-4">💰 あなたの報酬ダッシュボード</h1>

  <p class="fs-4">総報酬額：<strong>¥<%= totalReward %></strong></p>

  <table class="table">
    <thead>
      <tr>
        <th>広告名</th>
        <th>購入件数</th>
        <th>報酬合計</th>
      </tr>
    </thead>
    <tbody>
      <% rewards.forEach(r => { %>
        <tr>
          <td><%= r.title %></td>
          <td><%= r.purchase_count %></td>
          <td>¥<%= r.total_reward %></td>
        </tr>
      <% }) %>
    </tbody>
  </table>
</div>

<%- include('partials/footer') %>
